extends layout

block content
  h1= title
  p Welcome to #{title}
  - var image_names = ["blackrose.jpg","bluerose.jpg","greenrose.jpg","redrose.jpeg","yellowrosee.jpg"]
  - for(let i=0; i<3;i++){
    -var num = image_names[Math.floor(Math.random() * 5)]
    
    img(src= "/images/"+num, alt= num, style={width: "200",height: "200"})
  -}